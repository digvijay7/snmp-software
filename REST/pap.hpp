#include"log.hpp"
#include<vector>

// Static Function definiton to get log data and populate a vector using it

namespace sn{
  bool process_n_populate(std::vector<log> &data,std::vector<std::vector<std::string> >  & fro_tro); 
}

